---
import { twMerge as merge } from 'tailwind-merge';

interface Props {
	as?: string;
	class?: string;
}

const { as = 'div', class: className } = Astro.props;
const TagName = as;
---

<TagName class={merge('container', className)}>
	<slot />
</TagName>
