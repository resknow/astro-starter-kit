---
import { Icon as AstroIcon } from 'astro-icon';

interface Props {
	name: string;
	class?: string;
}

const { name, class: className } = Astro.props;
---

<AstroIcon name={name} class:list={[className]} />

<style>
	svg {
		width: 1em;
		height: 1em;
	}
</style>
